# Copy to server/.env (git-ignored) and fill in. Never commit secrets.
PORT=5000
JWT_SECRET=replace-with-a-long-random-string
SALT=replace-with-a-random-value
DATABASE_URL=mysql://user:password@localhost:3306/mydb
